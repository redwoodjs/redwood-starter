export const stopAsyncIteration = (asyncIterable) => {
    const method = asyncIterable[Symbol.asyncIterator];
    const asyncIterator = method.call(asyncIterable);
    if (asyncIterator.return) {
        asyncIterator.return();
    }
};
